/**
 * 효율적이지 못한 코드
 * StringBuffer을 사용할것
 */

    public String getState4() {
        String state = "";
        state += heater ? "H" : "h";
        state += blower ? "B" : "b";
        state += cooler ? "C" : "c";
        state += hiTempAlarm ? "H" : "h";
        state += loTempAlarm ? "L" : "l";   
        return state;
    }